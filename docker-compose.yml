version: "3.8"

services:
  frontend:
    container_name: next-admin
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3005:3000"
    env_file:
      - .env
    restart: always
