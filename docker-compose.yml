version: "3.8"

services:
  # Next.js frontend
  class-admin-dev:
    container_name: class-admin-dev
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "13000:13000"
    env_file:
      - .env
    restart: always
    networks:
      - class-software-dev-network
networks:
  class-software-dev-network:
    external: true
